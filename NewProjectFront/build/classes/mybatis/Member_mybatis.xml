<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace속성:매퍼파일의 완전한경로 .xml는 생략 -->
<!-- ※ibatis와는 다르게 id값에 .(dot)를 사용 못한다. -->
<mapper namespace="mybatis.Notice_mybatis">


 	<!-- id속성에 지정한 값을 자바코드(DAO계열 클래스)에서 
           식별자로 사용 -->
 	<insert id="createSimple_Mem" parameterType="java.util.Map" >
 			INSERT INTO SIMPLE_MEM VALUES(#{smem_id},#{smem_name},#{smem_tel},#{smem_pwd},sysdate)
 	</insert>
 	
	<select id="login" parameterType="java.util.Map" resultType="int">
			SELECT COUNT(*) FROM SIMPLE_MEM WHERE SMEM_ID=#{smem_id} AND SMEM_PWD=#{smem_pwd}
	</select>
	
	
</mapper>